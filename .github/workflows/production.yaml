name: Vercel Production Deployment
env:
  VERCEL_ORG_ID: https://vercel.com/sudosubh's projects
  VERCEL_PROJECT_ID: https://vercel.com/sudosubh's projects/instastay
on:
  push:
    branches:
      - main
jobs:
  Deploy-Production:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --v86SjaxcE8vqfU35zdVd63cI
      - name: Build Project Artifacts
        run: vercel build --prod --v86SjaxcE8vqfU35zdVd63cI
      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --v86SjaxcE8vqfU35zdVd63cI
